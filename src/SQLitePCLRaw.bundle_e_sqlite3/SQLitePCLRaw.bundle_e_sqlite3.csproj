<Project Sdk="Microsoft.NET.Sdk">

  <!-- This is just a dummy csproj for dotnet pack -->

  <PropertyGroup>
    <TargetFramework>netstandard2.0</TargetFramework>
    <NoBuild>true</NoBuild>
    <IncludeBuildOutput>false</IncludeBuildOutput>
    <NuspecFile>pack.nuspec</NuspecFile>
    <NuspecProperties>version=$(Version);lib_e_sqlite3_package_reference_version=$(lib_e_sqlite3_package_reference_version)</NuspecProperties>
  </PropertyGroup>

</Project>

